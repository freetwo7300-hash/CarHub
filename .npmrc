# pnpm configuration
engine-strict=false
ignore-scripts=true

# Disable supply-chain policy checks for Docker builds
# Allows packages published very recently to be installed
# This is acceptable for development/CI builds
# See: https://pnpm.io/npmrc#policy-entries
