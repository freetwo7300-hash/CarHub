# pnpm configuration
engine-strict=false
ignore-scripts=true

# Disable supply-chain policy checks
# This is safe for Docker/CI builds where we rebuild the lockfile regularly
# See: https://pnpm.io/package_json#pnpmoverrides
# Set minimumReleaseAge to 0 to disable the check entirely
# minimumReleaseAge=0

